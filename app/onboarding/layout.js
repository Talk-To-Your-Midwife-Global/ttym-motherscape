import {PageSlideAnimator} from "../_components";

export default function OnboardingLayout({children}) {
    return (
        <PageSlideAnimator>
            {children}
        </PageSlideAnimator>
    )
}